esphome:
  name: $name
  comment: $device_description
  project:
    name: $project_name
    version: $project_version

esp32:  
  board: wesp32
  framework:
    type: arduino
    version: recommended

substitutions:
  status_led_inverted: "false"
